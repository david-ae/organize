import { createAction } from '@ngrx/store';

export const addItem = createAction('[Add Item] Item');
export const updateItem = createAction('[Update Item] Item');
export const getItem = createAction('[Get Item] Item');
