import { InventoryFilterPipe } from './inventory-filter.pipe';

describe('InventoryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new InventoryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
